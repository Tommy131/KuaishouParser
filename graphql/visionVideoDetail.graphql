query visionVideoDetail($videoId: String) {
  visionVideoDetail(photoId: $videoId) {
    photo {
      likeCount
      realLikeCount
      coverUrl
      photoUrl
      viewCount
    }
    tags {
      type
      name
    }
  }
}