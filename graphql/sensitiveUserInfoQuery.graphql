query sensitiveUserInfoQuery($principalId: String) {
  sensitiveUserInfo(principalId: $principalId) {
    kwaiId
    originUserId
    constellation
    cityName
    counts {
      fan
      photo
      open
      private
    }
  }
}