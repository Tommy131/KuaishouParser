query visionVideoDetail($photoId: String) {
  visionVideoDetail(photoId: $photoId) {
    photo {
      likeCount
      realLikeCount
      coverUrl
      photoUrl
      viewCount
    }
    tags {
      type
      name
    }
  }
}